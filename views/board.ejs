<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/reset-css@5.0.2/reset.min.css" />
  <link rel="stylesheet" href="/css/index.css" />
</head>

<body>
  <%- include('nav.ejs') %>
  <% for (var i = 0; i < posts.length; i++){ %>

  <div class="white-box">
    <div class="list-box">
      <a href="/boardDetail/<%= posts[i].id %>">
        <h4><%= posts[i].title %></h4>
        <p><%= posts[i].description %></p>
      </a>
      <a href="/boardEdit/<%= posts[i].id %>"><button>수정</button></a>
      <a href="/boardDelete/<%= posts[i].id %>"><button>삭제</button></a>
    </div>

    <% } %>
</body>



</html>